<%= form_with(model: @project, url: project/create, local:true) do |f| %>
    <%= f.text_field :name %>
    <%= f.text_area :description %>
    <%= f.date_select :start_on %>
    <%= f.date_select :end_on %>
    <%= f.select :state, @states, {include_blank: true} %>
    <%= f.submit %>
<% end %>